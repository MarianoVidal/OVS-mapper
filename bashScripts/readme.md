## Scripts de Bash
Pequeños scripts que hice para automatizar cosas del proyecto.

### graph_ave.sh
Hace exactamente lo mismo que 'make graph', pero además puede cargar otro<br>
archivo csv si se lo pasa como argumento, y utiliza el argumento '-A' por defecto.<br> 
El archivo input por defecto sigue siendo el mismo, _./input/input.csv_